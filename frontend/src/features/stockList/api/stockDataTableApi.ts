import { load as fetchItems } from "$shared/api/fetchItems";
import { load as fetchLocations } from "$shared/api/fetchLocations";
import { load as fetchStocks } from "$shared/api/fetchStocks";
import { load as fetchCategories } from "$shared/api/fetchCategories";

export { fetchItems, fetchLocations, fetchStocks, fetchCategories };
